{
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.19"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.2.0"
  },
  "scripts": {
    "prepareFrontend": "npx hardhat compile && node scripts/copyABIs.js && npx hardhat run scripts/deploy.js --network localhost"
  }
}